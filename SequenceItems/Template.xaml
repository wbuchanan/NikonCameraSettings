<ResourceDictionary
    x:Class="NikonCameraSettings.SequenceItems.Template"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:NikonCameraSettings.SequenceItems"
    xmlns:mini="clr-namespace:NINA.View.Sequencer.MiniSequencer;assembly=NINA.Sequencer"
    xmlns:nina="clr-namespace:NINA.View.Sequencer;assembly=NINA.Sequencer">

    <!--  The definition of the plugin test instruction style  -->
    <!--  The DataType has to be defined as the instruction in context  -->
    <DataTemplate DataType="{x:Type local:SetStorageLocation}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Change Storage Location: " />
                    <ComboBox
                        Width="100"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding StorageLocationSettings}"
                        SelectedItem="{Binding SelectedStorageLocationSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetMonitorOnOff}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Turn Camera Monitor On/Off: " />
                    <ComboBox
                        Width="100"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding MonitorSettings}"
                        SelectedItem="{Binding SelectedMonitorSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetAutoISOControl}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Turn Camera Monitor On/Off: " />
                    <ComboBox
                        Width="100"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding AutoISOSettings}"
                        SelectedItem="{Binding SelectedISOSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetImageStabilization}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Change Image Stabilization Setting: " />
                    <ComboBox
                        Width="100"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding VRSettings}"
                        SelectedItem="{Binding SelectedVRSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:StartPixelMapping}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Start Pixel Mapping the Sensor" />
                    <TextBlock
                        Margin="20,0,0,0"
                        VerticalAlignment="Center"
                        FontStyle="Italic"
                        Text="Note: The camera will be busy for 30 seconds or more.  I recommend only using this in your start up block." />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:StartFocusShift}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Text="Capture Focus Shift Stack: " />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="Polling Interval in milliseconds: " />
                        <TextBox
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="{Binding PollInterval, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            ToolTip="Be sure that the polling interval is short enough to capture some of your images being captured, otherwise it may result in errors." />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="Max Polling Iterations: " />
                        <TextBox
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="{Binding MaxPollIterations, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            ToolTip="This number should be at least equal to: 1 + ((Exposure Time per Image in ms * Number of Images) / Poll Interval) to ensure there will be a sufficient number of attempts made." />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Text="Status: " />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="Total Shots in Stack: " />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="{Binding TotalShots}" />
                        <TextBlock
                            Margin="25,0,0,0"
                            VerticalAlignment="Center"
                            Text="Shots Completed: " />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="{Binding CompletedShots}" />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="Shots Remaining: " />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="{Binding RemainingShots}" />
                    </StackPanel>
                    <StackPanel Orientation="Vertical">
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            FontStyle="Italic"
                            Text="Note: You must set the focus shift parameters in the camera. There is no SDK access to those parameters." />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            FontStyle="Italic"
                            Text="Additionally, this is only recommended to capture foreground (terrestrial) features." />
                    </StackPanel>
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <!--
    <DataTemplate DataType="{x:Type local:SetNmsIptcData}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Margin="0,2" Orientation="Vertical">
                    <StackPanel Margin="0,0,0,4" Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,4,0"
                            VerticalAlignment="Center"
                            Text="Preset Slot:"
                            ToolTip="NMS-IPTC preset slot (1-10) to write the metadata to" />
                        <TextBox
                            Width="30"
                            Margin="0,0,10,0"
                            VerticalAlignment="Center"
                            Text="{Binding PresetSlot, UpdateSourceTrigger=PropertyChanged}"
                            ToolTip="Camera preset slot number (1-10) for NMS-IPTC data. Values outside this range are clamped." />
                        <CheckBox
                            Margin="0,0,5,0"
                            VerticalAlignment="Center"
                            Content="Auto-Embed"
                            IsChecked="{Binding ActivatePreset, UpdateSourceTrigger=PropertyChanged}"
                            ToolTip="Activate this preset for auto-embedding into captured images (overrides any other active preset)" />
                    </StackPanel>
                    <WrapPanel Orientation="Horizontal">
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Title:"
                                ToolTip="IPTC Object Name / Title (max 256 bytes)" />
                            <TextBox
                                Width="120"
                                MaxLength="256"
                                Text="{Binding ObjectName, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:05 — Object Name" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Headline:"
                                ToolTip="IPTC Headline — brief synopsis (max 256 bytes)" />
                            <TextBox
                                Width="120"
                                MaxLength="256"
                                Text="{Binding Headline, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:105 — Headline" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Caption:"
                                ToolTip="IPTC Caption/Abstract — detailed description (max 2000 bytes)" />
                            <TextBox
                                Width="180"
                                MaxLength="2000"
                                Text="{Binding Caption, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:120 — Caption/Abstract" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Event ID:"
                                ToolTip="IPTC Event Identifier — session or event ID (max 64 bytes)" />
                            <TextBox
                                Width="100"
                                MaxLength="64"
                                Text="{Binding EventId, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:12 — Event Identifier" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Byline:"
                                ToolTip="IPTC Byline — photographer/creator name (max 256 bytes)" />
                            <TextBox
                                Width="120"
                                MaxLength="256"
                                Text="{Binding Byline, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:80 — Byline (Creator)" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Byline Title:"
                                ToolTip="IPTC Byline Title — creator's job title (max 256 bytes)" />
                            <TextBox
                                Width="100"
                                MaxLength="256"
                                Text="{Binding BylineTitle, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:85 — Byline Title" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="City:"
                                ToolTip="IPTC City — location city name (max 256 bytes)" />
                            <TextBox
                                Width="100"
                                MaxLength="256"
                                Text="{Binding City, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:90 — City" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="State:"
                                ToolTip="IPTC Province/State (max 256 bytes)" />
                            <TextBox
                                Width="80"
                                MaxLength="256"
                                Text="{Binding State, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:95 — Province/State" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Country:"
                                ToolTip="IPTC Country name (max 256 bytes)" />
                            <TextBox
                                Width="100"
                                MaxLength="256"
                                Text="{Binding Country, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:101 — Country" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Category:"
                                ToolTip="IPTC Category — 3-character ASCII code (e.g. SCI, ENV)" />
                            <TextBox
                                Width="40"
                                MaxLength="3"
                                Text="{Binding IptcCategory, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:15 — Category (3 chars max, ASCII only)" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Supp. Cat.:"
                                ToolTip="IPTC Supplemental Categories (max 256 bytes)" />
                            <TextBox
                                Width="100"
                                MaxLength="256"
                                Text="{Binding SuppCat, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:20 — Supplemental Categories" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Credit:"
                                ToolTip="IPTC Credit — provider or image owner (max 256 bytes)" />
                            <TextBox
                                Width="100"
                                MaxLength="256"
                                Text="{Binding Credit, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:110 — Credit" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Source:"
                                ToolTip="IPTC Source — original source (max 256 bytes)" />
                            <TextBox
                                Width="100"
                                MaxLength="256"
                                Text="{Binding Source, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:115 — Source" />
                        </StackPanel>
                        <StackPanel Margin="0,1,8,1" Orientation="Horizontal">
                            <TextBlock
                                Margin="0,0,3,0"
                                VerticalAlignment="Center"
                                Text="Writer/Editor:"
                                ToolTip="IPTC Writer/Editor — who wrote the caption (max 256 bytes)" />
                            <TextBox
                                Width="100"
                                MaxLength="256"
                                Text="{Binding WriterEditor, UpdateSourceTrigger=PropertyChanged}"
                                ToolTip="IPTC IIM 2:122 — Writer/Editor" />
                        </StackPanel>
                    </WrapPanel>
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>-->

    <DataTemplate DataType="{x:Type local:SetXmpIptcData}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel
                    Margin="0,2"
                    IsEnabled="{Binding AreControlsEnabled}"
                    Orientation="Vertical">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,4,0"
                            VerticalAlignment="Center"
                            Text="XMP Slot:" />
                        <ComboBox
                            MinWidth="50"
                            VerticalAlignment="Center"
                            ItemsSource="{Binding AvailableSlots}"
                            SelectedItem="{Binding PresetSlot, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock
                            Margin="8,0,4,0"
                            VerticalAlignment="Center"
                            Text=" | " />
                        <CheckBox
                            Margin="4,0,0,0"
                            VerticalAlignment="Center"
                            Content="Auto-Embed"
                            IsChecked="{Binding ActivatePreset, UpdateSourceTrigger=PropertyChanged}"
                            ToolTip="Activate this preset for auto-embedding into captured images. Only one preset can be active at a time." />
                    </StackPanel>

                    <StackPanel Margin="0,3,0,0" Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,0,0"
                            VerticalAlignment="Center"
                            Text="{Binding GpsStatusDisplay}"
                            ToolTip="GPS location from NINA's Astrometry Settings (Options &gt; General). Data is included in every XMP write." />
                        <TextBlock
                            Margin="8,0,8,0"
                            VerticalAlignment="Center"
                            Text=" | " />
                        <TextBlock
                            VerticalAlignment="Center"
                            Text="{Binding TelescopeStatusDisplay}"
                            ToolTip="Telescope pointing data (RA, Dec, Alt, Az) is captured at execution time and included if the telescope is connected." />
                        <TextBlock
                            Margin="8,0,8,0"
                            VerticalAlignment="Center"
                            Text=" | " />
                        <TextBlock
                            VerticalAlignment="Center"
                            Text="{Binding WeatherStatusDisplay}"
                            ToolTip="Weather data (temperature, humidity, pressure, etc.) is captured at execution time and included if a weather device is connected." />
                    </StackPanel>
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetActiveDLighting}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Active D-Lighting: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding ActiveDLightingSettings}"
                        SelectedItem="{Binding SelectedActiveDLightingSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetVignetteControl}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Vignette Control: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding VignetteControlSettings}"
                        SelectedItem="{Binding SelectedVignetteControlSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetDiffractionCompensation}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Diffraction Compensation: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding DiffractionCompensationSettings}"
                        SelectedItem="{Binding SelectedDiffractionCompensationSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetAutoDistortionControl}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Auto Distortion Control: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding AutoDistortionSettings}"
                        SelectedItem="{Binding SelectedAutoDistortionSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetSingleAFPriority}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Single AF Priority: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding SingleAFPrioritySettings}"
                        SelectedItem="{Binding SelectedSingleAFPrioritySetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetContinuousAFPriority}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Continuous AF Priority: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding ContinuousAFPrioritySettings}"
                        SelectedItem="{Binding SelectedContinuousAFPrioritySetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetMeteringMode}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Metering Mode: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding MeteringModeSettings}"
                        SelectedItem="{Binding SelectedMeteringModeSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetExposureMode}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Exposure Mode: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding ExposureModeSettings}"
                        SelectedItem="{Binding SelectedExposureModeSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetSilentMode}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Silent Mode: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding SilentModeSettings}"
                        SelectedItem="{Binding SelectedSilentModeSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetFileNumberSequence}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set File Number Sequence: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding FileNumberSequenceSettings}"
                        SelectedItem="{Binding SelectedFileNumberSequenceSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetAFModeLiveView}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set AF Mode (Live View): " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding AFModeLiveViewSettings}"
                        SelectedItem="{Binding SelectedAFModeLiveViewSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetAFSubjectDetection}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set AF Subject Detection: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding AFSubjectDetectionSettings}"
                        SelectedItem="{Binding SelectedAFSubjectDetectionSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetMenuBank}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Select Menu Bank: " />
                    <ComboBox
                        Width="100"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding MenuBankSettings}"
                        SelectedItem="{Binding SelectedMenuBankSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetImageSize}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Image Size: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding ImageSizeSettings}"
                        SelectedItem="{Binding SelectedImageSizeSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetColorSpace}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Color Space: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding ColorSpaceSettings}"
                        SelectedItem="{Binding SelectedColorSpaceSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetNoiseReduction}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Noise Reduction: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding NoiseReductionSettings}"
                        SelectedItem="{Binding SelectedNoiseReductionSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetHighISONoiseReduction}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set High ISO Noise Reduction: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding HighISONoiseReductionSettings}"
                        SelectedItem="{Binding SelectedHighISONoiseReductionSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetPictureControl}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Picture Control: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding PictureControlSettings}"
                        SelectedItem="{Binding SelectedPictureControlSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetHDRMode}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set HDR Mode: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding HDRModeSettings}"
                        SelectedItem="{Binding SelectedHDRModeSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetBracketingType}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Bracketing Type: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding BracketingTypeSettings}"
                        SelectedItem="{Binding SelectedBracketingTypeSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetFlickerReduction}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Flicker Reduction: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding FlickerReductionSettings}"
                        SelectedItem="{Binding SelectedFlickerReductionSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetShutterSound}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Shutter Sound: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding ShutterSoundSettings}"
                        SelectedItem="{Binding SelectedShutterSoundSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetCameraInclinationMode}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Camera Inclination Mode: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding CameraInclinationModeSettings}"
                        SelectedItem="{Binding SelectedCameraInclinationModeSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetLiveViewMode}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Live View Mode: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding LiveViewModeSettings}"
                        SelectedItem="{Binding SelectedLiveViewModeSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetAFModeRestrictions}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set AF Mode Restrictions: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding AFModeRestrictionsSettings}"
                        SelectedItem="{Binding SelectedAFModeRestrictionsSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetSlot2SaveMode}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Card Slot 2 Save Mode: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding Slot2SaveModeSettings}"
                        SelectedItem="{Binding SelectedSlot2SaveModeSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetEVInterval}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Exposure Comp Stop Size: " />
                    <ComboBox
                        Width="120"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding EVIntervalSettings}"
                        SelectedItem="{Binding SelectedEVIntervalSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetExposureDelay}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Exposure Delay: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding ExposureDelaySettings}"
                        SelectedItem="{Binding SelectedExposureDelaySetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetShootingMode}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Shooting Mode: " />
                    <ComboBox
                        Width="150"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding ShootingModeSettings}"
                        SelectedItem="{Binding SelectedShootingModeSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetEnableCopyright}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Enable Copyright Info: " />
                    <ComboBox
                        Width="80"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding EnableCopyrightSettings}"
                        SelectedItem="{Binding SelectedEnableCopyrightSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetEnableComment}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Enable Comment Embedding: " />
                    <ComboBox
                        Width="80"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding EnableCommentSettings}"
                        SelectedItem="{Binding SelectedEnableCommentSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetLockCamera}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Lock/Unlock Camera Controls: " />
                    <ComboBox
                        Width="80"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding LockCameraSettings}"
                        SelectedItem="{Binding SelectedLockCameraSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetStarlightView}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Starlight View: " />
                    <ComboBox
                        Width="80"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding StarlightViewSettings}"
                        SelectedItem="{Binding SelectedStarlightViewSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetEnableBracketing}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Enable Bracketing: " />
                    <ComboBox
                        Width="80"
                        Margin="5,0,0,0"
                        ItemsSource="{Binding EnableBracketingSettings}"
                        SelectedItem="{Binding SelectedEnableBracketingSetting, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetArtistName}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Artist Name: " />
                    <TextBox
                        Width="200"
                        Margin="5,0,0,0"
                        VerticalAlignment="Center"
                        Text="{Binding ArtistName, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetCopyrightInfo}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set Copyright Info: " />
                    <TextBox
                        Width="200"
                        Margin="5,0,0,0"
                        VerticalAlignment="Center"
                        Text="{Binding CopyrightInfo, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:SetUserComment}">
        <nina:SequenceBlockView DataContext="{Binding}">
            <nina:SequenceBlockView.SequenceItemContent>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="Set User Comment: " />
                    <TextBox
                        Width="200"
                        Margin="5,0,0,0"
                        VerticalAlignment="Center"
                        Text="{Binding UserComment, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </nina:SequenceBlockView.SequenceItemContent>
        </nina:SequenceBlockView>
    </DataTemplate>

</ResourceDictionary>